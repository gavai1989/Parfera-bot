# PARFERA — catalog rebuild

Catalog rebuilt directly from the supplied client price list.
The source product name is preserved verbatim in `name`; no invented `base_name` field is used.
This package is for search/catalog validation and is not yet a Render deployment.
